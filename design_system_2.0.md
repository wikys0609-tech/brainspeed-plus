# 두뇌 스피드 테스트 2.0 — 디자인 시스템

> 컨셉: **오락실(아케이드) 레트로**. 옛날 오락실 게임기 앞에 앉은 느낌을 세련되게.
> 원칙: 무드는 레트로하게, 텍스트는 명확하게. 개성은 프레임·색·디테일에서, 가독성은 절대 희생 안 함.

---

## 1. 컨셉 한 줄

"동네 오락실의 두뇌 게임기." 화면 전체가 하나의 아케이드 캐비닛(게임기)이고,
사용자는 그 앞에 앉아 버튼을 누르는 느낌. 촌스러움이 아니라 "정갈한 복고".

---

## 2. 색 팔레트 (모드별 테마 색)

베이스는 공통, 각 모드에 들어가면 그 모드의 테마 색이 화면을 지배함.

### 공통 베이스
- 배경(캐비닛): `#1A1526` (어두운 자주빛 남색 — 오락실 조명 꺼진 느낌)
- 패널/카드: `#2A2440` (살짝 밝은 자주빛)
- 기본 텍스트: `#F5EFE0` (따뜻한 아이보리 — 오래된 화면의 흰색)
- 흐린 텍스트: `#9B93B5`
- 픽셀 그림자/보더: `#0D0A14` (거의 검정, 픽셀 테두리용)

### 모드별 테마 색 (각 모드의 주인공 색)
- **상식 퀴즈 = 청록(cyan)**: 주 `#2EC4C4`, 밝은 `#5FE0E0`, 어두운 `#1A8A8A`
- **빠른 계산 = 앰버(amber)**: 주 `#F2A83B`, 밝은 `#FFC85F`, 어두운 `#C47D1A`
- **지리 추론 = 그린(green)**: 주 `#5FB84A`, 밝은 `#82D96B`, 어두운 `#3D8A2E`

### 공통 강조/상태
- 정답: `#5FB84A` (초록)
- 오답: `#E8544E` (레드)
- 하이라이트/포인트: `#FF5FA2` (핫핑크 — 오락실 네온 느낌, 아주 절제해서 포인트로만)

---

## 3. 타이포그래피

원칙: **둥글고 귀여운 레트로 폰트**를 제목·숫자·라벨에, **가독성 좋은 폰트**를 문제·선택지 본문에.
(레트로 폰트로 문제 전체를 도배하면 60초 안에 읽기 어려워짐 — 절대 금지)

- **디스플레이(제목/점수/타이머)**: 둥근 레트로 감성 폰트.
  한글 지원되는 둥근 폰트 추천 — 예: "DungGeunMo"(둥근모, 픽셀+둥근 느낌), "Galmuri"(갈무리, 둥근 픽셀),
  또는 둥근 산세리프 굵은 웨이트. 웹폰트 용량이 크면 제목·숫자에만 제한적으로 사용.
- **본문(문제/선택지/힌트)**: 가독성 최우선. 깔끔한 고딕/산세리프(예: Pretendard, 노토산스KR).
  레트로는 이 텍스트를 감싸는 프레임·색으로 표현하고, 글자 자체는 또렷하게.
- **숫자(점수·타이머·계산 문제)**: 디스플레이 폰트 사용. 오락실 스코어보드 느낌으로 큼직하게.

타입 스케일: 제목 특대, 점수·타이머 대, 문제 중대, 선택지 중, 라벨·힌트 소.

---

## 4. 시그니처 요소 (이 게임을 기억하게 할 단 하나)

**"아케이드 캐비닛 프레임".** 모든 화면이 오락실 게임기 화면처럼 보이도록,
화면 바깥을 두꺼운 픽셀 테두리(베젤)로 감싸고, 안쪽이 "게임 스크린"이 됨.

- 픽셀 스타일의 두꺼운 보더(2~4px, 계단식 모서리 = 픽셀 라운드).
- 버튼은 오락실 물리 버튼처럼: 눌리면 아래로 살짝 들어가는(press-down) 입체감.
- 점수·타이머는 상단에 "스코어보드"처럼: 7세그먼트/픽셀 숫자 느낌.
- 이 프레임 하나에 개성을 집중하고, 나머지는 깔끔하게.

지리 퀴즈 이미지는 이 캐비닛 프레임 안에 "게임 화면 속 사진"처럼 들어감 (UI 중심, 이미지는 보조).

---

## 5. 화면별 적용

- **시작 화면**: 오락실 게임기 타이틀 화면 느낌. 게임 로고(레트로 폰트) 크게,
  아래 모드 선택 버튼 3개(상식=청록, 계산=앰버, 지리=그린 색으로 구분). 전체이용가 등급 표시.
- **모드 화면**: 진입하면 해당 모드 테마 색이 프레임·포인트에 적용. 상단 스코어보드(점수·타이머).
  가운데 문제, 아래 버튼. 정답 초록 플래시, 오답 레드 플래시.
- **지리 추론 화면**: 캐비닛 프레임 안에 이미지, 그 아래 힌트 슬롯(잠긴 힌트는 "?"로, 열면 내용 공개),
  주관식 입력창. 힌트 열 때마다 오락실 효과음 느낌.
- **결과 화면**: 오락실 "GAME OVER / SCORE" 화면 오마주. 점수 큼직하게, 최고기록 갱신 시
  "NEW RECORD!" 픽셀 연출. 리더보드(순위 보기) 버튼.

---

## 6. 모션 (절제해서)

- 버튼 누름: press-down 물리 버튼 느낌 (필수, 오락실 감성 핵심).
- 정답/오답: 짧은 플래시 + 살짝 흔들림. 과하지 않게.
- 점수 증가: 스코어보드 숫자가 올라가는 롤링 효과.
- 화면 전환: 오락실 CRT 켜지는 느낌(살짝)도 가능하나, reduced-motion 존중하고 무겁지 않게.
- 남용 금지: 애니메이션이 많으면 로딩·성능·검수(10초 규칙)에 악영향.

---

## 7. 지켜야 할 제약 (앱인토스 검수)

- 세로 모드 고정, 풀스크린, Safe Area 준수.
- 10초 내 로딩 — 레트로 폰트/텍스처가 무거우면 안 됨. 웹폰트는 최소한으로, 서브셋 사용.
- 텍스트 가독성 최우선 — 문제·선택지는 레트로 폰트로 도배하지 말 것.
- 색 대비 충분히 (어두운 배경 + 밝은 텍스트라 대비는 유리함).

---

## 8. 한 줄 요약 (Antigravity에 전달용)

"어두운 자주빛 배경의 오락실 게임기 컨셉. 화면을 픽셀 베젤로 감싸 아케이드 캐비닛처럼 보이게 하고,
모드별로 테마 색(상식=청록, 계산=앰버, 지리=그린)을 적용. 제목·점수·숫자는 둥근 레트로 폰트,
문제·선택지 본문은 가독성 좋은 고딕. 버튼은 눌리는 물리 버튼 느낌. 개성은 캐비닛 프레임에 집중,
텍스트 가독성은 절대 희생하지 않음."
